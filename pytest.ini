[pytest]
DJANGO_SETTINGS_MODULE = fakestore.settings
# Restrict discovery to explicit test_* patterns to avoid directory/package name collisions
python_files = test_*.py *_tests.py
pythonpath = backend
addopts = -ra
